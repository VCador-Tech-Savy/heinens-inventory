apiVersion: v1
kind: Service
metadata:
  name: inventory-frontend-service
  namespace: heinens
spec:
  selector:
    app: inventory-frontend
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
